import { ModulePlaceholder } from "@/shared/components/admin/ModulePlaceholder";

export default function SettingsPage() {
  return (
    <ModulePlaceholder
      title="Settings"
      description="Company profile, users, roles, and platform preferences will live here."
    />
  );
}
