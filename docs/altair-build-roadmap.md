# Altair 16-Panel Build Roadmap

Source: the 16-panel reference sheet (dark/brass visual language) covering the whole app. Work through in this order so nothing gets built out of sequence or forgotten. Update the Status column as work progresses — this file is meant to be edited over time.

| # | Panel | Status | Notes |
|---|---|---|---|
| 1 | Dashboard (Mission Control) | ✅ Done | MC v2 — dark light-on-dark cards, live data, sidebar restyled, trial banner relocated. Component library extracted (mc-surface, SectionHeader). |
| 2 | Customers (list) | ✅ Done | MC v2 surface. Header row: Total Customers/New This Month (plain) + Active/Needs Info/Inactive/Past (clickable filter pills, correct isCustomerActiveQueue count). Contact + Last Service columns added. Old segmented tab bar removed. |
| 3 | Customer Profile (360° view) | ✅ Done | Reshaped existing Customer 360 page. Side rail (Customer info + Opportunities), consolidated top row, 4-stat row (Total Spent uses canonical payment-ledger sum, Jobs Completed, Avg. Ticket, Last Service). Tabs: Jobs/Estimates/Invoices/Payments/Notes/Files/Equipment/Activity. Trial badge removed from top bars. |
| 4 | Leads | ✅ Done | MC v2 surface, filter pills: New, Contacted, Qualified, Estimate ready, Needs contact, Converted, Past. Table: Lead Name/Phone/Source/Status/Created/Next Follow-Up/Last Activity. No Value column — logged as a gap instead of faked. |
| 5 | Jobs (list) | ✅ Done | MC v2 surface. Today/All toggle + six canonical status pills. Type column added. Today view rebuilt with shared JobScheduleRow, chronological, brass "Now" divider. Fixed unassigned-jobs sort bug. |
| 6 | Job Details | ✅ Done | MC v2 surface, condensed header, workflow bar restyled, real tab gating (Description/Equipment/Materials/Photos/Billing/History), Job Summary sidebar from real data. No Tasks checklist or Est. Duration — logged as gaps. |
| 7 | Estimates (list) | ✅ Done | MC v2 surface. StatStrip: Draft/Sent/Approved/Declined + Past. EstimateQueueTabs retired, orphan components deleted. |
| 8 | Estimate Details | ✅ Done | MC v2 header, document card as source of truth, side rail Customer panel, honest $0 tax display. Did NOT add fake "Convert to Job". Fixed duplicate document DOM render bug. |
| 9 | Invoices (list) | ✅ Done | MC v2 surface. StatStrip: Draft/Sent/Partially paid/Overdue/Paid + Past. Orphan components deleted. Fixed overdue-sync bug on Customer Profile's invoice tab. |
| 10 | Invoice Details | ✅ Done | MC v2 header, honest $0 states, Payments section confirmed real. Fixed same duplicate document DOM bug as Estimate Details. |
| 11 | Payments (new page) | ✅ Done | New route. MC v2 surface. StatStrip: Total collected/This week/This month. Table with pagination. Added to Money nav group. |
| 12 | Dispatch | ✅ Done | Fixed overlap bug, horizontal Gantt rows, dark-theme exception (breaks light MC v2 standard), fixed-width start-time job blocks (no fake duration), static Mapbox job-location map (not live GPS), merged map+grid onto one page, fixed no-scroll bug, condensed header. Full month calendar deferred to Schedule (#14). |
| 13 | Technicians | ✅ Done | New page, added to Work nav group. Roster from listCompanyMembers. Real time-clock status breakdown (not invented "On-the-road/On-leave"). Honest "No reviews yet" empty state. Real per-technician referral codes (member_share_code). |
| 14 | Schedule | ✅ Done | Dark ops-command treatment via dispatchMissionClasses, matching Dispatch. Week and month views share one ScheduleDayCell component (density="week"/"month"). Real month-range query wired (getMonthBoundsInTimeZone + addDaysToDateOnly, client-bucketed). Verified against real data, not fabricated. Day click navigates to /dispatch?date=YYYY-MM-DD. Dispatch now supports ?date= param. No technician availability/leave overlay — confirmed not buildable, correctly omitted. Month-view dates use a rounded-md chip so the date doesn't compete visually with the job-count number. |
| 15 | Time Clock | ✅ Done | MC v2 reshape of existing `/time-clock` — real data throughout. Crew status via Technicians' `attachTeamMemberTimeStates`/`countTechnicianTimeStates`. Today hours/open/stale shift math extracted into shared `buildShiftTimeTrackingSummary`, now called by both Time Clock and Reports' Shift Time card — confirmed identical live numbers. Table: Technician/Clock In/Clock Out/Duration/Status, nested break/job reflected as On break/On job. Self clock in/out + missed-clock-out correction kept; no admin-clocks-others or edit-closed-shift. `/time` (labor review) deliberately untouched, separate scope. Fast-follow: total hours this week, not wired in v1. |
| 16 | Settings | ✅ Done | MC v2 shell, 6-tab scope (Company/Billing/Users/Integrations/Notifications/Preferences). Company: editable name/trade/timezone/phone/email/full address via updateCompanyProfileAction. Billing: /settings/subscription reshaped, Stripe Connect payments nested as sub-section, /settings/payments redirects. Users: existing team UI reshaped/renamed, still at /settings/team. Integrations: Stripe + Facebook/Instagram linked, honest 'not available yet' for the rest — no fake Connect buttons. Notifications: real inbox preview, no fabricated toggles. Preferences: timezone-only via updateCompanyTimezoneAction. Overview/Documents/System Check kept under More. All 16 panels on the original roadmap now complete. |

## Also in progress (not part of the 16-panel set)

- **Marketing (founder-only automation)** — 🔄 In progress. Real Facebook OAuth Connect flow, founder screenshot upload/capture tooling, comparison screenshot tool. Post Now (Facebook/Instagram publish) is code-complete but blocked on Meta app console config (needs correct use cases added, real domain instead of localhost, "Ready for testing" status on pages_manage_posts/instagram_basic/instagram_content_publish before retrying Connect).
- **Homepage hero (unrelated to this project)** — found uncommitted/unwired during commit audit. Left uncommitted deliberately, status unknown, needs confirmation whether it's active work or safe to clean up.
- **Reports** — 🔄 In progress. Dark report-surface conversion done for all sections. Remaining: visual tiering, sparklines on Tier 1 cards, Operations Snapshot 4-column merge, Recommended Actions section (real records only), Business Score/health-grade rubric (paused pending a dedicated scoring conversation).

## Working notes

- Dashboard and Reports are done reference points — reuse their components/tokens.
- Surface style decision: all panels from #2 onward use MC v2 (mc-surface.ts), not Reports' report-surface.ts. Reports keeps its own distinct treatment.
- Dispatch exception: dark ops-command treatment instead of light MC v2, deliberately chosen for real-time boards. Schedule (#14) follows the same treatment.
- Time-block width rule: on any time-axis board, job blocks are fixed-width start-time markers, never stretched to imply duration, since scheduled_end/job duration doesn't reliably exist.
